# Local dev: point the Next.js frontend at your local backend.
NEXT_PUBLIC_API_URL=http://localhost:3001
# Shared secret with the backend — must exactly match backend API_KEY,
# otherwise all /api/news calls return 401.
NEXT_PUBLIC_API_KEY=change-me-to-match-backend-api-key
